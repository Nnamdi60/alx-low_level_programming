#include "main.h"

/**
 * _islower - print only a line
 * @c: contains comparable values
 * Description: prints all lowercase letters
 * Return: Always(0)
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
